<%= form_with(model: teacher_availability) do |form| %>
  <% if teacher_availability.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(teacher_availability.errors.count, "error") %> prohibited this teacher_availability from being saved:</h2>

      <ul>
        <% teacher_availability.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :tname, style: "display: block" %>
    <%= form.text_field :tname %>
  </div>

  <div>
    <%= form.label :day, style: "display: block" %>
    <%= form.date_field :day %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
